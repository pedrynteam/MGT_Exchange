@model IEnumerable<MGT_Exchange.TicketAPI.MVC.CommentTicket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ticket)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ticket.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.commentTicketId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.commentTicketId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.commentTicketId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
